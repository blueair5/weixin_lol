<!--pages/legend/legend.wxml-->
<view class="container">
<view class="show" 
wx:for="{{hero}}"
wx:key="heroId"
>
<navigator url="/pages/hero/hero?id={{item.heroId}}">{{item.name}}</navigator>

</view>
</view>
